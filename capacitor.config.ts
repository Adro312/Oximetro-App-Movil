import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sw17_contenedores',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
